{
  "rules": {
    ".read": false,
    ".write": false,
    // Allow read and write access to authenticated users for their own data
    // "users": {
    //   "$uid": {
    //     ".read": "$uid === auth.uid",
    //     ".write": "$uid === auth.uid"
    //   }
    // }

    // Example: Restrict access to specific data within "Data"
    // "users": {
    //   "$uid": {
    //     "Data": {
    //       "BibleProgress": {
    //         ".read": "$uid === auth.uid",
    //         ".write": "$uid === auth.uid"
    //       }
    //     }
    //   }
    // }
  }
}