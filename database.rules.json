{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
    // Example: Restrict access to specific data within "Data"
    // "users": {
    //   "$uid": {
    //     "Data": {
    //       "BibleProgress": {
    //         ".read": "$uid === auth.uid",
    //         ".write": "$uid === auth.uid"
    //       }
    //     }
    //   }
    // }
  }
}